    import XCTest
    @testable import SwiftLoggedPrint

    final class SwiftLoggedPrintTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(SwiftLoggedPrint().text, "Hello, World!")
        }
    }
